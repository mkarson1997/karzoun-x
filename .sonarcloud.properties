# SonarQube Cloud automatic-analysis scope.
# The distributable KARZOUN-X package is src/karzoun_x (see pyproject.toml).
# Research artifacts, paper builders, frozen experiment scripts, and generated
# publication material remain versioned for reproducibility but are not shipped
# as runtime package code and should not define the product-code quality gate.
sonar.sources=src/karzoun_x
sonar.tests=tests
sonar.sourceEncoding=UTF-8
sonar.python.version=3.11
