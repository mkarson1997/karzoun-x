# Experiments

Store immutable experiment configuration files here. Generated results belong under `experiments/results/` and are ignored by git until a publication-ready result bundle is intentionally curated.

Every experiment should record the repository commit, dataset provenance, model identifier, seed, hardware, and exact configuration.
